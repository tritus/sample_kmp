query UserNameQuery($accountId: ID!) {
  me @nonnull {
    account(id: $accountId) @nonnull {
      id @nonnull
      accountUser @nonnull {
        id @nonnull
        firstname
        lastname
      }
    }
  }
}
