query PaymentMethodDetails($accountId: ID!, $paymentMethodId: ID!) {
  me {
    account(id: $accountId) {
      paymentMethod(id: $paymentMethodId) {
        id
        cardName
        cardExpiration
        cardLast4Digits
        cardScheme
        processoutTokenRef
      }
    }
  }
}
